apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: my-app
  namespace: default
spec:
  components:
    - name: my-app
      type: webservice
      properties:
        image: taweizhong235/nginx-test:v1 # {"$imagepolicy": "default:image-gitops"}
